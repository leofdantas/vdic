% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_builder.R
\name{.filter_embedding_vocab}
\alias{.filter_embedding_vocab}
\title{Filter embedding vocabulary by spellcheck and stopwords}
\usage{
.filter_embedding_vocab(
  embeddings_path,
  stopwords = NULL,
  spellcheck = FALSE,
  language = NULL,
  verbose = TRUE
)
}
\arguments{
\item{embeddings_path}{Path to embeddings file}

\item{stopwords}{Character vector of stopwords to remove, or NULL}

\item{spellcheck}{Logical. If TRUE, filters using hunspell spell checking}

\item{language}{Language code for hunspell dictionary (e.g., "en", "pt", "es").
If NULL, defaults to "en". Used to select the correct hunspell dictionary
so that spell checking works for non-English embeddings.}

\item{verbose}{Logical, print progress messages}
}
\value{
Path to filtered embeddings file (temp file, or original if no filtering)
}
\description{
Reads the full embeddings file and filters out stopwords and non-words
(via hunspell spell checking). Writes a temporary filtered embeddings file
that downstream functions can use directly, so no other function needs
spellcheck/stopword logic.
}
\keyword{internal}
